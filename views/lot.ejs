<% include header.ejs %>
    <% include lotModal.ejs %>
        <div class="p-4 col-12">
            
            <h4 class=" font-weight-bold page-header">Stock</h4>
            <hr>
            <table class="table table-bordered table-sm">
                <thead style="background-color: #757574 ; color : white">
                    <tr>
                        <th>ID</th>
                        <th>Date</th>
                        <th>ProductID</th>
                        <th>QTY</th>
                        
                    </tr>
                </thead>
                <% stocks.forEach(function(abc){ %>
                <tbody style="background-color: #ccccc8">
                
                    <tr>
                        <td><%= abc._id %></td>
                        <td><%= abc.date %></td>
                        
                        
                        <td>
                            <div class="row m-0">
                                <div class="d-flex">
                                    <button type="button" class="btn btn-outline-dark" id="UpdateBtn">
                <i class="fa fa-pen"></i></button>
                                </div>
                                <div class="d-flex">
                                    <button class="btn btn-outline-dark" id="DelBtn">
                <i class="fa fa-trash"></i></button>
                                </div>
                            </div>
                        </td>
                    </tr>                    
                    </tbody>
                    <% include lotModalupdate.ejs %>
                    <% include lotDel.ejs %>
                    <% }) %>
            </table>

                    <button type="button" class="buttonAdd button5 addMenu" id="AddBtn">
                        Add
                    </button>

                    <script src="/javascripts/modal.js"></script>
                    <% include footer.ejs %>
